✅ Beginner Practice Set – Array Basics (10 Questions)
Take n integers as input and store in a list. Print the list.

Print the sum of all elements in a list.

Find the maximum and minimum element in a list (without max()/min()).

Count even and odd numbers in a list.

Reverse the list without using reverse() or slicing.

Find the index of a given element (without using .index()).

Check if a list is sorted in ascending order.

Remove all zeros from a list.

Square each element and store in a new list.

Shift all negative elements to the left and positives to the right (without sorting).